//select elements that you need to either attach things to
// or check


const dropdown = document.getElementById('animal-farm')
const pig = dropdown.children[0].value;
if (pig === 'pig'){
    console.log('oink oink');
}

const animalChoice = () => {
    cons
}